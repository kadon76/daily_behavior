<% provide(:title, 'Join mailing list') %>
<h1>Join our mailing list</h1>

<div class="row">
	<div class="span6 offset3">
		<%= form_for(@prospect) do |f| %>
			<%= render 'fields', f: f %>
			<%= f.submit "Join our list", class: "btn btn-large btn-primary" %>
		<% end %>
	</div>
</div>